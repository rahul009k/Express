const names = ["Rahul", "Amit", "Neha", "Priya"];
for(const name of names){
    console.log(name);
}
log
// this i will use when i have get only values from array or object. 

// where as for loop i will use to get the index as well as the value from array or object.