# for...in Loop

## 📖 Definition

The `for...in` loop is used to iterate over the keys (property names) of an object. On each iteration, the loop variable holds the current key as a string.

```javascript
for (const key in object) {
    // code
}
```

---

# Why do we need for...in?

When you want to loop over all properties of an object and access both the key and value.

---

# Syntax

```javascript
for (const key in objectName) {
    console.log(key, objectName[key]);
}
```

---

# Example

```javascript
const laptop = {
    brand: "Dell",
    ram: "16GB",
    storage: "512GB",
    price: 75000
};

for (const key in laptop) {
    console.log(key, laptop[key]);
}
```

Output

```
brand Dell
ram 16GB
storage 512GB
price 75000
```

---

# Accessing Only Keys

```javascript
for (const key in laptop) {
    console.log(key);
}
```

Output

```
brand
ram
storage
price
```

---

# Accessing Only Values

```javascript
for (const key in laptop) {
    console.log(laptop[key]);
}
```

Output

```
Dell
16GB
512GB
75000
```

---

# for...in vs Object.keys() / Object.entries()

| | for...in | Object.keys() | Object.entries() |
|--|---------|--------------|-----------------|
| Returns | Keys one by one | Array of keys | Array of pairs |
| Inherited props | ✅ Includes | ❌ Excludes | ❌ Excludes |
| Use case | Simple iteration | Array operations | Key + value |

---

# for...in vs for...of

| | for...in | for...of |
|--|---------|---------|
| Used for | Objects (keys) | Arrays, strings, iterables |
| Gives | Keys | Values |

---

# Inherited Properties Warning

`for...in` also iterates over inherited properties. Use `hasOwnProperty()` to filter.

```javascript
for (const key in laptop) {
    if (laptop.hasOwnProperty(key)) {
        console.log(key, laptop[key]);
    }
}
```

---

# Best Practices

✅ Use `for...in` for simple object iteration.

```javascript
for (const key in laptop) {
    console.log(key, laptop[key]);
}
```

Prefer `Object.entries()` with `for...of` for cleaner code.

```javascript
for (const [key, value] of Object.entries(laptop)) {
    console.log(key, value);
}
```

Use `hasOwnProperty()` if the object might have inherited properties.

---

# Common Mistakes

## Using for...in on arrays

Wrong

```javascript
const colors = ["Red", "Green", "Blue"];

for (const key in colors) {
    console.log(key);
}
```

Output

```
0
1
2
```

This gives indices, not values. Use `for...of` for arrays.

Correct

```javascript
for (const color of colors) {
    console.log(color);
}
```

---

## Forgetting to use bracket notation

Wrong

```javascript
for (const key in laptop) {
    console.log(laptop.key);
}
```

Output

```
undefined
undefined
...
```

Correct

```javascript
for (const key in laptop) {
    console.log(laptop[key]);
}
```

---

# Real-world Example

```javascript
const user = {
    name: "Rahul",
    email: "rahul@example.com",
    role: "admin",
    age: 26
};

for (const key in user) {
    console.log(`${key}: ${user[key]}`);
}
```

Output

```
name: Rahul
email: rahul@example.com
role: admin
age: 26
```

---

# Interview Questions

1. What is the `for...in` loop used for?
2. What does the loop variable hold in `for...in`?
3. What is the difference between `for...in` and `for...of`?
4. Does `for...in` iterate over inherited properties?
5. How do you access the value inside a `for...in` loop?
6. Why should you avoid using `for...in` on arrays?
7. What is `hasOwnProperty()`?
8. What is the difference between `for...in` and `Object.keys()`?
9. Can you use `for...in` on a string?
10. When would you prefer `Object.entries()` over `for...in`?

---

# Interview Definition

> The `for...in` loop iterates over the enumerable property keys of an object. On each iteration, the loop variable holds the current key as a string, and the value can be accessed using bracket notation. Unlike `Object.keys()`, `for...in` also includes inherited properties, so `hasOwnProperty()` should be used when needed.

---

# Practice Exercises

1. Create a laptop object and loop over it using `for...in`.
2. Print only the keys using `for...in`.
3. Print only the values using `for...in`.
4. Print both key and value in a formatted string.
5. Use `hasOwnProperty()` inside a `for...in` loop.
6. Demonstrate why `for...in` should not be used on arrays.
7. Compare `for...in` with `Object.entries()` on the same object.
8. Create a user object and print all properties using `for...in`.
9. Count the number of properties using `for...in`.
10. Write a function that accepts any object and prints all its key-value pairs.
