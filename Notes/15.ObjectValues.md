# 🟦 Object.values()

---

## 📖 Definition

`Object.values()` is a built-in JavaScript method that returns an **array of all the property values** of an object.

```javascript
Object.values(object)
```

> 💡 It gives you only the **values**, not the keys.

---

## 💻 Example

```javascript
const laptop = {
    brand: "Dell",
    ram: "16GB",
    storage: "512GB",
    price: 75000
};

console.log(Object.values(laptop));
```

**Output**

```
[ 'Dell', '16GB', '512GB', 75000 ]
```

---

## 🔄 Looping Over Values

```javascript
for (const value of Object.values(laptop)) {
    console.log(value);
}
```

**Output**

```
Dell
16GB
512GB
75000
```

---

## ➕ Summing Numeric Values

```javascript
const scores = {
    math: 90,
    science: 85,
    english: 78
};

const total = Object.values(scores).reduce((sum, val) => sum + val, 0);

console.log(total); // 253
```

---

## 📊 keys() vs values() vs entries()

| Method | Returns |
|--------|---------|
| `Object.keys()` | `['brand', 'ram', 'storage', 'price']` |
| `Object.values()` | `['Dell', '16GB', '512GB', 75000]` |
| `Object.entries()` | `[['brand','Dell'], ['ram','16GB'], ...]` |

---

## ✅ Best Practices

✅ Use `Object.values()` when you only need the data, not the property names.

✅ Use with `reduce()` to calculate totals.

```javascript
Object.values(scores).reduce((sum, val) => sum + val, 0);
```

---

## ⚠️ Common Mistakes

### ❌ Modifying the returned array does not change the original object

```javascript
const values = Object.values(laptop);
values[0] = "HP";

console.log(laptop.brand); // Dell — unchanged
```

---

## 🌍 Real-world Example

```javascript
const product = {
    name: "Laptop",
    brand: "Dell",
    ram: "16GB",
    price: 75000
};

console.log("Product details:");
Object.values(product).forEach(value => console.log("-", value));
```

**Output**

```
Product details:
- Laptop
- Dell
- 16GB
- 75000
```

---

## 🎤 Interview Questions

1. What does `Object.values()` return?
2. What is the difference between `Object.keys()` and `Object.values()`?
3. How do you loop over the values of an object?
4. Does modifying the returned array affect the original object?
5. What does `Object.values()` return for an empty object?
6. How do you sum all numeric values in an object?
7. What is the data type of the return value?
8. Does `Object.values()` include inherited properties?
9. Can you use `Object.values()` on an array?
10. What is the difference between `Object.values()` and `for...in`?

---

## 💡 Interview Definition

> `Object.values()` is a static method that returns an array of an object's own **enumerable property values**. It does not include keys or inherited properties. The returned array can be iterated, summed, or processed like any other array.

---

## 🏋️ Practice Exercises

1. Create a laptop object and print all its values using `Object.values()`.
2. Loop over the values using `for...of`.
3. Loop over the values using `forEach`.
4. Create a scores object and calculate the total using `Object.values()` and `reduce()`.
5. Compare `Object.keys()` and `Object.values()` output on the same object.
6. Create a user object and print all values.
7. Check if a specific value exists using `Object.values()` and `includes()`.
8. Create an empty object and call `Object.values()` on it.
9. Demonstrate that modifying the returned array does not change the original object.
10. Write a function that accepts an object and returns the sum of all numeric values.
